name: clinical-triage-env
version: 1.0.0
description: Medical triage RL environment.

tasks:
  - easy
  - medium
  - hard

endpoints:
  health: GET /health
  reset: POST /reset
  step: POST /step
  state: GET /state
